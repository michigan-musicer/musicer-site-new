import '../Main.scss';
import { showCategory, showCategorySpecifier } from '../include/showCategory'; 

const Ceritifcations = (props: showCategorySpecifier) => {
  return <div></div>
}

export default Ceritifcations;